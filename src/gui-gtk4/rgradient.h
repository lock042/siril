#ifndef SRC_GUI_RGRADIENT_H_
#define SRC_GUI_RGRADIENT_H_

#include <glib.h>

/* Open the dialog in amend mode for history record @record_id
 * (nde_editors registry, convergence C5b). */
gboolean rgradient_open_amend(gint64 record_id);

#endif /* SRC_GUI_RGRADIENT_H_ */
