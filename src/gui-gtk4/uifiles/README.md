Cambalache Widget Library
=========================

Siril uses a custom class SirilImageView which Cambalace needs to know about.
It is defined in SirilImageView-1.0.xml: this file must be copied into
~/.cambalache/catalogs so that Cambalache knows about it and can parse siril.ui
without errors.
